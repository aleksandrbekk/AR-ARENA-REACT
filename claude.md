# ПРАВИЛА ДЛЯ АГЕНТОВ AR ARENA

## ⛔ ГЛАВНОЕ ПРАВИЛО

**НИКОГДА не предлагать пользователю делать что-то вручную.**

Александр — координатор. Он НЕ:
- Заходит в Supabase Dashboard
- Копирует SQL в редактор
- Открывает браузер для действий
- Делает что-либо руками кроме проверки в Telegram

---

## 📱 TELEGRAM MINI APP — FULLSCREEN

**AR ARENA работает в FULLSCREEN режиме!**

### Что это значит:
- Приложение занимает весь экран
- Сверху НЕТ стандартного header Telegram
- Системные кнопки (закрыть, назад) находятся В ОБЛАСТИ ПРИЛОЖЕНИЯ

### ⛔ ЗАПРЕЩЕНО:
- Размещать элементы в самом верху экрана (0-60px)
- Делать кнопки/текст без учёта safe-area
- Игнорировать отступы на новых страницах

### ✅ ОБЯЗАТЕЛЬНО:
```css
/* Отступ сверху для safe-area */
padding-top: env(safe-area-inset-top, 60px);

/* Или минимум */
padding-top: 60px;
```

### Структура страницы:
```html
<div class="page-container">
  <!-- Пустая зона 60px для системных кнопок -->
  <div class="safe-area-top"></div>

  <!-- Контент начинается ПОСЛЕ safe-area -->
  <div class="content">
    ...
  </div>
</div>
```

### CSS:
```css
.page-container {
  min-height: 100vh;
  padding-top: env(safe-area-inset-top, 60px);
  padding-bottom: env(safe-area-inset-bottom, 20px);
  background: #0a0a0a;
}
```

---

## 📱 ПОДИУМ / ПРЕВЬЮ ПЕРСОНАЖА

**Правила размещения персонажа на подиуме:**

- Голова персонажа НЕ ДОЛЖНА быть выше кнопки "Закрыть"
- Верхние 60px — зона системных кнопок Telegram
- Персонаж начинается НИЖЕ этой зоны (используй `pt-12` или больше)
- Фон/градиент может идти от края, но контент (персонаж) — ниже

### Пример:
```tsx
{/* Градиент от края экрана */}
<div style={{ background: gradient, height: '40vh' }}>
  {/* Персонаж с отступом сверху */}
  <div className="pt-12">
    <img src="..." className="max-h-[28vh]" />
  </div>
</div>
```

---

## 🤖 АВТОМАТИЗАЦИЯ

Если задача требует действия в браузере/сервисе — используй MCP:

| Задача | Инструмент |
|--------|------------|
| SQL в Supabase | Playwright → SQL Editor |
| Проверка сайта | Playwright / Control Chrome |
| Файлы на сервере | SSH через bash |
| Деплой | scp команда |

---

## ✅ ПРАВИЛЬНО
```
Агент: "Выполняю SQL через Playwright..."
[выполняет]
Агент: "Готово! Результат: 15 записей добавлено"
```

## ❌ НЕПРАВИЛЬНО
```
Агент: "Вот SQL, зайди в Dashboard и выполни"
Агент: "Открой браузер и..."
Агент: "Скопируй это и вставь в..."
```

---

## 🎨 ДИЗАЙН-СИСТЕМА

- **Фон:** #0a0a0a (чёрный)
- **Акцент:** #FFD700 (золотой)
- **Градиент:** linear-gradient(135deg, #FFD700, #FFA500)
- **Шрифты:** 14px / 18px / 24px
- **Иконки:** ТОЛЬКО из /icons/ (не emoji, не CDN)
- **Эффекты:** glassmorphism, glow

---

## 🔑 ДОСТУПЫ

- **Supabase:** syxjkircmiwpnpagznay.supabase.co
- **Сервер:** 91.229.11.228
- **SSH пароль:** T9WLiUYq0Uv0Yn2W
- **Путь:** /www/ararena.pro/

---

## 🎯 ЕДИНСТВЕННОЕ ЧТО ДЕЛАЕТ АЛЕКСАНДР

1. Проверяет результат в Telegram Mini App
2. Говорит "+" или описывает баг
3. Принимает решения по продукту

ВСЁ ОСТАЛЬНОЕ — АВТОМАТИЗАЦИЯ АГЕНТАМИ.

---

## 📋 ЧЕКЛИСТ ПЕРЕД СОЗДАНИЕМ СТРАНИЦЫ

- [ ] Есть padding-top: 60px (safe-area)?
- [ ] Фон #0a0a0a?
- [ ] Иконки из /icons/?
- [ ] Нет элементов в верхних 60px?
- [ ] Деплой включает все файлы (HTML + иконки)?
