# Current State

## 🚀 Active Task
**Giveaway система - ГОТОВА**

### ✅ Применено в Supabase (04.01.2026)
- **SQL функции для розыгрышей:**
  - `generate_giveaway_result(p_giveaway_id)` - генерация результатов (20→5→winners)
  - `distribute_giveaway_prizes(p_giveaway_id)` - распределение призов победителям
  - `run_giveaway_draw(p_giveaway_id)` - комбинированная функция (генерация + распределение)
  - Колонки: `draw_results`, `winners`, `prizes_distributed`

### ⏳ Можно тестировать
1. Создай розыгрыш в Admin Panel → Giveaways
2. Нажми "Провести розыгрыш"
3. Должно сработать без ошибок

---

## 📌 Previous Task
**Фикс регистрации пользователей Mini App + UsersTab удаление**

### 🔍 Расследование: Почему только 17 юзеров?

**Результат проверки базы:**
- `users` (Mini App): **14** записей (после тестов с удалением)
- `bot_users` (бот): **1527** записей

**Найденная проблема:**
- RLS политика `Users can update own profile` требует `auth.uid() = id`
- Мы используем Telegram Auth, не Supabase Auth → `auth.uid()` = NULL
- INSERT работает (первое открытие), но UPDATE падает (повторные визиты)

**Фикс (ПРИМЕНЁН):**
1. ✅ Создан API `/api/upsert-user.js` (service role key на сервере)
2. ✅ AuthProvider вызывает API вместо прямого Supabase
3. ✅ **PricingPage тоже вызывает API** (для не-админов на странице тарифов)
4. ✅ Задеплоено на Vercel

### ✅ Сделано сегодня
- **Удаление юзеров в UsersTab:**
  - Кнопка удаления в профиле игрока
  - Массовое удаление через чекбоксы
  - Модалка подтверждения
- **Убран LVL столбец** — не используется
- **Убрано онбординг-модалка** — из App.tsx
- **PricingPage регистрирует юзеров** — fetch к /api/upsert-user

---

### ✅ Done (Сегодня)
- **App CRM (UsersTab) - полностью переписан**:
  - Таблица игроков: аватар, имя, уровень (бейдж), AR, BUL, билеты, последний визит
  - Сортировка по кликам на колонки (level, AR, BUL, last_seen)
  - Пагинация по 20 записей
  - Поиск по telegram_id, username, имени
  - Статистика: игроков, средний уровень, всего AR, активных за 24ч

- **Профиль игрока (отдельный экран)**:
  - Шапка: аватар, имя, username, telegram_id, уровень, даты
  - Балансы AR/BUL с кнопками ±
  - Билеты с кнопкой "Добавить"
  - История транзакций (последние 20)
  - Скины (купленные, активный отмечен)
  - Ферма (оборудование и уровни)
  - Кнопка "Написать в Telegram"

- **Админские операции**:
  - Начисление/списание AR/BUL (модалка)
  - Добавление билетов на розыгрыш (выбор из активных)
  - Отправка сообщения через бота

### 🏗️ Ранее сделано
- **FinalBattle баги**: truncate ники, русский заголовок, защита от loop
- **BackButton конфликт**: SELF_MANAGED_PAGES fix
- **Полуфинал "СВЕТОФОР"**: звуки, offset рулетки, Array(50), призы
- **Dev Test Panel** (`/live-test`): 4 кнопки для тестирования
- **Live Arena Components**: Tour1Drum, Tour2Squeeze, SemifinalTraffic, FinalBattle
- **SFX & Haptics**: useArenaSounds (Web Audio), useArenaHaptics (TMA)

### ⏳ Готово к тестированию
- Открой https://ararena.pro в Telegram
- Зайди в Admin Panel → Юзеры
- Проверь:
  - [ ] Таблица показывает всех игроков с аватарами
  - [ ] Сортировка работает (клик на колонку)
  - [ ] Клик по строке → профиль игрока
  - [ ] Начисление AR/BUL работает
  - [ ] Добавление билетов работает
  - [ ] Кнопка "Назад" возвращает к списку
