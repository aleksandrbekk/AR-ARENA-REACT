---
description: 
---

# /rollback — Откат на предыдущую версию

Когда что-то сломалось после деплоя:

1. git log --oneline -5 — покажи последние коммиты
2. Спроси: "Откатить последний коммит или выбрать конкретный?"
3. После подтверждения:
   - git revert HEAD --no-edit (для последнего)
   - или git revert <hash> --no-edit (для конкретного)
4. git push

⚠️ НЕ использовать git reset --hard — это опасно
✅ Использовать git revert — безопасный откат

Ответ:
⏪ Откачено: [какой коммит]
🚀 Задеплоено: новая версия без проблемного кода