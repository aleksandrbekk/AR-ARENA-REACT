# 🌾 FARM ROADMAP

## Статус: 🔴 Не начато (0%)

**Последнее обновление:** 12.12.2025

---

## 📊 ОБЩИЙ ПРОГРЕСС

```
░░░░░░░░░░░░░░░░░░░░  0%
```

- ✅ Готово: Механика описана, legacy код есть
- 🔄 В работе: —
- ⏳ Запланировано: Всё (база данных, UI, логика)

---

## 🎯 КОНЦЕПЦИЯ

**Ферма** — пассивный доход в валюте **BUL**.

### Основные элементы:
1. **Локации** — разные места добычи (шахта, завод, ферма и т.д.)
2. **Оборудование** — улучшения для увеличения дохода
3. **Прогресс-бар** — накопление BUL в реальном времени
4. **Кнопка "Собрать"** — забрать накопленное

### Экономика:
- Каждая локация имеет `income_per_hour`
- Оборудование увеличивает `income_per_hour` на % или фиксированное значение
- Максимальное накопление без сбора: `max_accumulation` (например, 12 часов)
- Покупка локаций/оборудования — за BUL

---

## ✅ ГОТОВО

### Документация
- [x] **specs/FARM-SPEC.md** — детальная спецификация (планируется)
  - Описание механики накопления
  - Формулы дохода
  - Список локаций и оборудования

### Legacy код
- [x] **Vanilla версия** — готовый референс
  - Путь: `~/Desktop/AR ARENA VANILA ВЕРСИЯ/farm.html`
  - Можно взять UI/UX паттерны, анимации, тексты

---

## ⏳ ЗАПЛАНИРОВАНО

### База данных (АНЯ)
- [ ] **Проверить таблицы farm_* в Supabase**
  - `farm_locations` — список доступных локаций
  - `farm_equipment` — список оборудования
  - `user_farm_locations` — купленные локации пользователя
  - `user_farm_equipment` — купленное оборудование пользователя
  - `user_farm_progress` — прогресс накопления BUL

- [ ] **Проверить RPC функции**
  - `get_farm_income()` — текущий доход в час
  - `collect_farm_bul(telegram_id)` — собрать накопленное
  - `buy_farm_location(telegram_id, location_id)` — купить локацию
  - `buy_farm_equipment(telegram_id, equipment_id)` — купить оборудование
  - `calculate_accumulated_bul(telegram_id)` — сколько накопилось

- [ ] **Миграция если нужно**
  - Создать недостающие таблицы
  - Создать/обновить RPC функции
  - Заполнить начальные данные (список локаций/оборудования)

### UI (ВАЛЕРА)
- [ ] **Создать FarmPage.tsx**
  - Основная страница фермы
  - Навигация в `/farm`
  - Safe-area отступы (60px сверху)

- [ ] **Компонент LocationCard**
  - Карточка локации (название, иконка, доход)
  - Кнопка "Купить" (если не куплено)
  - Статус "Активна" (если куплено)
  - Цена в BUL

- [ ] **Компонент EquipmentCard**
  - Карточка оборудования (название, иконка, бонус)
  - Кнопка "Купить" (если не куплено)
  - Статус "Установлено" (если куплено)
  - Цена в BUL

- [ ] **Прогресс-бар накопления**
  - Анимированный прогресс-бар
  - Текущее накопленное BUL
  - Время до максимального накопления
  - Real-time обновление (каждую секунду)

- [ ] **Кнопка "Собрать"**
  - Золотая CTA кнопка (градиент)
  - Disabled если накопилось 0 BUL
  - Анимация при клике
  - Обновление баланса после сбора

- [ ] **Модалка выбора локации**
  - Список доступных локаций
  - Детали (доход, требования)
  - Подтверждение покупки

- [ ] **Модалка выбора оборудования**
  - Список доступного оборудования
  - Детали (бонус, требования)
  - Подтверждение покупки

### Логика (ВАЛЕРА + АНЯ)
- [ ] **Интеграция с Supabase**
  - Подключить RPC функции
  - Обработка ошибок (недостаточно BUL)
  - Обновление UI после покупки/сбора

- [ ] **Real-time накопление**
  - Таймер обновления прогресс-бара каждую секунду
  - Формула: `accumulated = income_per_hour * (hours_passed / 3600)`
  - Учёт `max_accumulation`

- [ ] **Оптимизация**
  - Кэширование данных локаций/оборудования
  - Debounce для API запросов
  - Lazy loading иконок

---

## 🎨 ДИЗАЙН

### Палитра
Следовать [rules/ar-arena-design.md](../rules/ar-arena-design.md):
- Фон: `#0a0a0a` с виньеткой
- Карточки: glassmorphism (`bg-zinc-900/50`, `backdrop-blur-md`)
- Кнопка "Собрать": золотой градиент `from-[#FFD700] to-[#FFA500]`
- Обводки: `border-yellow-500/20`

### Иконки
- Локации: `/public/icons/farm/locations/`
- Оборудование: `/public/icons/farm/equipment/`
- ⛔ НЕ использовать внешние библиотеки (lucide-react и т.д.)

### Прогресс-бар
```tsx
<div className="relative h-3 bg-zinc-800 rounded-full overflow-hidden">
  <div
    className="absolute h-full bg-gradient-to-r from-[#FFD700] to-[#FFA500]"
    style={{ width: `${progress}%` }}
  />
</div>
```

---

## 📝 ТЕХНИЧЕСКИЕ ДЕТАЛИ

### Таблицы (примерная структура)
```sql
farm_locations
├── id (PK)
├── name
├── description
├── icon_path
├── income_per_hour (BUL)
├── price_bul
├── required_level
└── max_accumulation_hours

farm_equipment
├── id (PK)
├── name
├── description
├── icon_path
├── bonus_type ('percentage' | 'fixed')
├── bonus_value
├── price_bul
└── required_location_id (FK)

user_farm_locations
├── user_id (FK → users)
├── location_id (FK → farm_locations)
└── purchased_at

user_farm_equipment
├── user_id (FK → users)
├── equipment_id (FK → farm_equipment)
└── purchased_at

user_farm_progress
├── user_id (PK, FK → users)
├── last_collected_at
└── accumulated_bul (вычисляемое)
```

### RPC функции (примерные сигнатуры)
```sql
-- Получить текущий доход в час
get_farm_income(p_telegram_id BIGINT)
RETURNS DECIMAL

-- Собрать накопленное BUL
collect_farm_bul(p_telegram_id BIGINT)
RETURNS JSON -- { success: boolean, collected: number, new_balance: number }

-- Купить локацию
buy_farm_location(p_telegram_id BIGINT, p_location_id INT)
RETURNS JSON -- { success: boolean, error?: string }

-- Купить оборудование
buy_farm_equipment(p_telegram_id BIGINT, p_equipment_id INT)
RETURNS JSON -- { success: boolean, error?: string }

-- Рассчитать накопленное BUL
calculate_accumulated_bul(p_telegram_id BIGINT)
RETURNS DECIMAL
```

---

## 🐛 ИЗВЕСТНЫЕ ПРОБЛЕМЫ

| Проблема | Приоритет | Статус |
|----------|-----------|--------|
| Нет таблиц в базе данных | Критический | ⏳ Нужна проверка |
| Нет UI страницы | Критический | ⏳ Запланировано |
| Нет иконок для локаций/оборудования | Средний | ⏳ Запланировано |

---

## 📚 LEGACY REFERENCE

Старая версия (vanilla JS):
```
~/Desktop/AR ARENA VANILA ВЕРСИЯ/farm.html
```

Используй как референс для:
- UI/UX паттернов
- Списка локаций и оборудования
- Формул расчёта дохода
- Текстов и описаний
- Анимаций прогресс-бара

---

## 🎯 СЛЕДУЮЩИЕ ШАГИ

1. **Проверить базу данных** — есть ли таблицы farm_*? (приоритет: критический)
2. **Создать/обновить RPC** — функции для сбора BUL, покупки локаций
3. **Создать FarmPage.tsx** — основная страница
4. **Компоненты карточек** — LocationCard, EquipmentCard
5. **Прогресс-бар** — real-time накопление BUL
6. **Подготовить иконки** — локации, оборудование
7. **Тестирование** — покупка, сбор, накопление

---

**Ответственные:**
- Backend (SQL/RPC): Аня
- Frontend (React): Валера
- Иконки/Дизайн: (уточнить)
- Координация: Александр
