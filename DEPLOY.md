# 🚀 Инструкция по деплою AR ARENA React

## Локальная разработка

```bash
npm run dev
```

Откроется http://localhost:5173
- Будет использоваться mock user (ID: 190202791)
- Данные загружаются из реальной Supabase БД

## Сборка для продакшна

```bash
npm run build
```

Результат в папке `dist/`:
- `dist/index.html` - главная страница
- `dist/assets/` - JS и CSS
- `dist/icons/` - иконки (BUL, энергия, скины)

## Деплой на сервер

### 🚀 Быстрый деплой (рекомендуется)

```bash
./deploy.sh
```

Этот скрипт автоматически:
1. Соберет проект (`npm run build`)
2. Задеплоит на сервер через SCP
3. Выведет результат

### Вариант 1: Ручной деплой (SCP) ✅ АКТУАЛЬНЫЙ

```bash
# Сборка
npm run build

# Деплой
sshpass -p 'T9WLiUYq0Uv0Yn2W' scp -r dist/* root@91.229.11.228:/www/ararena.pro/
```

**Сервер:** 91.229.11.228
**Путь:** /www/ararena.pro/
**URL:** https://ararena.pro
**Telegram:** @ARARENA_BOT

### Вариант 2: FTP/SFTP

Скопировать содержимое папки `dist/` на сервер в папку `/www/ararena.pro/`

### Вариант 3: Git + CI/CD

1. Push в репозиторий
2. На сервере: `git pull && npm install && npm run build`
3. Настроить nginx/apache на папку `dist/`

## Проверка в Telegram

1. Открыть бота в Telegram
2. Запустить WebApp
3. Открыть DevTools (на мобильном: подключить через USB debugging)
4. Console → должны быть логи:
   - "Telegram user:" с данными пользователя
   - "Game state:" с данными из БД
   - "=== HOME PAGE DEBUG ===" в Home.tsx

## Переменные окружения

Создать `.env` файл (если нужно):

```bash
VITE_SUPABASE_URL=your_supabase_url
VITE_SUPABASE_ANON_KEY=your_anon_key
```

**ВАЖНО:** Эти переменные уже жестко закодированы в `src/lib/supabase.ts`

## Устранение проблем

### SSH timeout
```bash
# Проверить доступ к серверу
ssh root@185.233.118.171

# Если порт не 22
ssh -p 2222 root@185.233.118.171
```

### Ошибки сборки
```bash
# Очистить кеш и пересобрать
rm -rf dist node_modules
npm install
npm run build
```

### Telegram WebApp не инициализируется
- Проверить что `telegram-web-app.js` загружается
- Проверить что приложение открыто именно из Telegram
- Использовать mock user для локальной разработки
